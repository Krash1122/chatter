{
  "name": "chatter-server",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "db:migrate": "node src/db/migrate.js"
  },
  "//": "Runtime dependencies deliberately live in the ROOT package.json, not here. Vercel builds api/index.js from the repo root, so anything this code requires has to be installed there; Node's module resolution walks up from server/src and finds it either way, which keeps local dev and the deployed function on one identical dependency tree.",
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
